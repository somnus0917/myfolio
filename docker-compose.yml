services:
  zola:
    image: ghcr.io/getzola/zola:v0.22.1
    working_dir: /site
    command: serve --interface 0.0.0.0 --port 8080 --base-url http://127.0.0.1:8080
    ports:
      - 8080:8080
    volumes:
      - .:/site
