name: Docker Slim

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Docker Slim retired
        run: echo "Docker Slim publishing was retired after the Zola migration."
